# Lumen Calculator

A distinctive React calculator interface with a Vite GUI, lightweight Express API, keyboard controls, memory buttons, and local browser history. It does not require a database.

## Run it

```powershell
npm run install:all
npm run dev
```

Open `http://localhost:5173`.
